# PocketForge 仓库工作契约（任何新会话首先遵守）

本项目 = 为 Windows 企业零权限环境构建「Agent 友好最小基础设施 + 门面层 + 通用数字员工」。
工作区可读文件拓扑 = 当下记忆；git log = 过去记忆的可追溯路径。

## 0. 新会话冷启动序列（必须按序执行）
1. 读 `STATE.md` → 知道"现在"：阶段、进行中、开放问题。
2. 需要过去的"为什么" → 查 `DECISIONS/`（ADR 索引在 `DECISIONS/README.md`）。
3. 需要外部事实 → 查 `docs/research/`（每条事实带来源与验证状态）。
4. 需要叙事/时间线 → `docs/journal/`（低权威：与 STATE 冲突时以 STATE 为准）。
5. 之后再看本文件余下约束。

## 1. 留痕纪律（每次工作会话结束前，缺一不可）
- 更新 `STATE.md`（"现在"永远正确）。
- 产生新决策 → 新建 `DECISIONS/00NN-*.md`（禁止改旧 ADR 正文，只能 supersede）。
- 关键外部事实首次确立/变更 → 更新 `docs/research/` 对应底稿。
- 追加 `docs/journal/YYYY-MM-DD-sNN-*.md` 会话记录（要点式，短）。
- `git commit` 并 push（2026-09-14 s85 用户拍板：变更必须提交且推送远端，本地提交不算落账），message 引用 ADR/STATE 变化；小步多次提交。

## 2. 硬约束（违反任意一条 = 立即停止并回到设计）
1. 目标机：企业 Windows 10/11，无本地管理员权限 → 一切组件解压即用，禁止 installer/MSI。
2. 无 Docker / WSL2 / Hyper-V → 排除一切容器化部署方案。
3. 便携文件夹交付，终端用户零技术背景 → "一次双击启动成功"是验收线。
4. 零系统污染：不写注册表、不写 PATH、不注册服务、不建计划任务；删文件夹 = 完全卸载；通过企业 EDR 审计。
5. 许可证白名单：MIT / Apache-2.0 / 公有领域（SQLite）/ PostgreSQL License、ISC（2026-09-05 用户批准扩列，ADR-0011）。BSL/GPL/SSPL/未明确 = 禁止。引入即记录于 research 底稿。
6. 封闭系统（PLM 等）无 API → 数据获取只走浏览器自动化。
7. 不预设业务场景：一切机制对任意业务通用。
8. 设施细节对 Agent 透明（Agent 只见稳定契约）；Agent 运行时禁止自研（采用开源，见 ADR）。

## 3. 事实纪律
- 关键断言必须带来源（URL / 本机复现命令）。
- 验证状态三级：`UNVERIFIED`（仅文档）→ `VERIFIED-DOC`（官方源核对）→ `VERIFIED-RUN`（本机跑通，含日期与版本）。
- 未验证的关键事实必须出现在 `STATE.md` 开放问题区。

## 3.5 状态 Schema 纪律（ADR-0009）
- 改任何自有状态文件（workspace-map/session-archive/providers/.forge 等）的格式 = 同一提交内必须：_schema 版本号 +1、注册表迁移步骤、迁移自测；无迁移的变更视为破坏性提交。
- sessions.db / schedule.json / faucet 数据归组件自迁移，永不手改。

## 4. 仓库分区
- `forge/` = 产品本体（交付物运行时树；打包只取它 + 许可证文本）。
- `tools/` = 开发侧工具（下载 manifest、校验、打包、e2e 测试）。
- `bin/`、`data/`、`dist/` 等大文件与运行时数据不入库（.gitignore），由 `tools/` 脚本 + manifest 可复现。
- `docs/`、`DECISIONS/`、`STATE.md` = 记忆层，永远入库。

## 5. 协作与语言
- 文档与注释：中文为主，技术名词保留英文。
- 面向妻子的最终文档（forge/ 内）：零术语。

## 6. 团队结构与协作（2026-08-28 起；v3 中文定稿）
本项目由产品团队自治推进。团队定义在**用户域** `~/.zcode/agents/`（服务全部项目），角色文件为 ZCode 子智能体格式，**中文提示词**，结构对齐 agency-agents 深度标准（身份记忆/核心使命/关键规则/输出格式），内容已通用化（方法论沉淀 + 通用陷阱清单），项目特有知识由本项目文档（STATE/ADR/research）在任务时注入：

| 角色 | 文件 | 一句话 |
|---|---|---|
| 主控（会话主代理） | 本文件 | 协调、拍板、落 ADR/STATE、调度子智能体 |
| pf-pm | pf-pm.md | 需求归约与路线裁决：先问三次为什么，用结果而非产出思考 |
| pf-researcher | pf-researcher.md | 取证研究员：归因必须单变量隔离；证据不足停止归约 |
| pf-engineer | pf-engineer.md | 实现工程师：最小 diff；每行改动可回答"为什么必须存在" |
| pf-qa | pf-qa.md | 测试审查：默认"需要返工"；一个残留状态是一个缺陷家族的可见成员 |

- 共同纪律：各角色文件含认知管线（取证→归约→构架→生成→收敛→校验）与输出法则，与 §0-§5 契约同源。
- 用户 = 内测期硬核技术型用户：给反馈/bug/真机环境/技术判断；产品路线与优先级由团队裁决落档。
- 内测期路线：验证优先于扩张（真机护航/首任务向导/使用统计/线路韧性）。
- 调度：单一职责任务派对应子智能体（prompt 自包含）；跨角色任务主控拆解；产出回写文档再 commit。

## 7. 信息架构规范（R1-R4 检查单，2026-09-07 s74 起 qa 审查正式依据；全文见 docs/verdicts/2026-09-07-ia-root-cure.md §3）

任何新增/改动的 UI 清单或面板，提交前逐条过：

- **R1 同页堆叠边界**：一个 modal/标签页 = 一个信息域 = 一类实体；两类实体禁止同容器。同一实体的不同生命周期阶段（已装 vs 可装）须显式 tab/分区，禁止滚动串接两个各自可滚的长清单。每视图 ≤1 个主清单区块。
- **R2 列表规模阶梯（机制内建制：机制出生即建，UI 随规模显隐）**（2026-09-18 r3 修订，裁决 docs/verdicts/2026-09-18-bloat-r3-built-in.md §3，取代 s74 原文）：
  - **机制存在性（出生即定，禁止触发式补建）**：任何新清单落地同批必须建成分页机制（pageSlice/pagerPaint，或 mem 式 50 渐进先例）+ 搜索框。允许的只是 UI 隐藏——pagerPaint 在 pages≤1 时自动不渲染（阈值内零视觉机制），但分页切片代码路径恒走。**「>N 再加分页」式触发声明 = 审查不过。**
  - **无上限增长清单（agent/对话驱动积累）一律三件套**：分页机制内建 + 搜索 + 至少一个结构维度过滤（chips 或天然分组或可被搜索命中的结构标签）。新清单提交必须声明增长引擎（谁/什么动作让它 +1）与三件套方案；增长引擎为 agent/对话驱动而缺任一件 = 审查不过。
  - **维度过滤准入**：维度=条目真实携带的生命周期状态/来源/类别字段，且混排损害找回（该被找到的沉底或淹没）。无真实维度的清单（如未装集合）声明「无维度」即可。chips 复用 made-chips/skills-chips 先例，键名与过滤字段随清单声明。
  - **规模档位（决定默认值与形态）**：0-8 条直接列；9-30 条搜索在场；31-100 条分页（卡片 12/页 SKILL_N、行型 30/页 PAGE_N，复用 pageSlice）+搜索；>100 条且有天然分组边界先分组（如数据表按应用分库）——无天然分组边界不强造分组。**任何上限截断必须留替代出路（搜索/完整视图），无出路的截断禁止上线。**
- **R3 配置归属**：配置跟着「改了它去哪看效果」走——放那个界面（或其内部收起的技术区）；禁止按技术相似性聚类。高频使用域不放裸露技术配置；技术配置允许同域但必须折叠默认不可见（同域不同层）。
- **R4 解释层义务**：面向妻子的清单，条目主名非中文人话（英文表名/包名）时必须附一行人话解释；解释来源优先级=系统元数据（建时落说明）>结构性事实（列数/类型/样例，**不得编造用途**）>来源+时间占位；无解释不得上线。机器/agent 生成的资产，人话身份是数据管线义务（建时落元数据），UI 只消费不猜。

存量清单机制声明（2026-09-18 r3 基线，机制内建制）：apps（agent 驱动）=搜索+分页 12/页+状态 chips；sched（对话驱动）=搜索+分页 30/页+暂停 chips；ins=搜索+分页 12/页+来源 chips；store=搜索+分页 12/页（无维度：未装集合）；mcp=搜索+分页（用户自配目录，已装/未装行内可辨）；db=搜索+行级分页 30/页+按库分组（桥 200 表护栏维持）；mem=搜索+50 渐进+分类标签可搜索（chips 化前置=建时人话元数据，裁决 2026-09-18 §4-4）；made=类型 chips+搜索+分页；侧栏归档/活跃与全部面板=搜索+分页+视图/状态 chips；ext 3 行 0-8 档；pr 提示词=搜索+分页 30/页。

## 8. 测试纪律（2026-09-14 s85 起，用户拍板；背景=冷启编码审计发现 dev 栈验证盖不住交付物缺陷）

任何对 PocketForge 的测试/验证，必须模拟真实用户路径，禁止只跑 dev 栈代替交付物验证：

1. **解压安装**：从 `dist/` 出厂 zip 全新解压到干净目录 → 走 `.cmd` 启动器冷启 → 按验收线核对。禁止直接在 `forge/` 源码树上跑 bootstrap 冒充交付验证。
2. **升级安装**：旧版本包先安装并产生数据 → 新版本包按发布说明的升级路径覆盖/就位 → 冷启 → 验证数据保留与新面生效。
3. 执行细则（安装路径变体、抓字节证据、判卷口径）见 docs/runbooks/2026-09-14-cold-start-codec-audit.md。

### 8.1 约定测试目录（2026-09-15 s93 用户拍板；所有会话/子智能体必须遵守）

- **全新安装测试的约定位置 = `C:\PF-TEST\<场景名>`**（例如 `C:\PF-TEST\cold`、`C:\PF-TEST\upgrade`）。这是唯一认可的沙盒根：好找、好清、不会误删用户数据。
- 测试完毕**必须整目录删除**（`C:\PF-TEST` 下不留任何场景残留），并核对零进程/零端口残留（词典：process-compose/postgres/faucet/nats-server + 端口 8790/8091/8099/5432/4222/8222）。
- 任何其它 `C:\PF-*` 目录（PF-F1、PF-V、PF-QA 等临时命名）**禁止再创建**——历史上散落的临时目录已全部清理过一次，不要再制造第二轮。
- dev 树（`C:\ZCodeWorks\PocketForge\forge`）与用户树（如 `C:\PocketForge`）**永远不是测试沙盒**，不得在其上做安装/升级/破坏性验证。
