# 2026-08-19 s01b — P2 冒烟（四组件单件全通）

- 下载 5 个 pinned 二进制 + sha256（tools/checksums.txt）；fetch.sh 曾在 nats-cli 后静默截断，逐个补齐（教训：长下载脚本要 `set -e`+显式断言，后续 manifest 驱动重写）。
- 冒烟细节与坑全记录于 `docs/runbooks/p2-verification-log.md`（11 项）。
- 重大确认：GOOSE_PATH_ROOT 完全收敛；faucet --data-dir/--foreground/--host 是便携三件套参数；NATS conf 路径必须正斜杠；goose 模型名 `/` 分隔符限制。
- 下一步：process-compose 全栈联动（含热重载）→ P2b browser-use。
