# 2026-08-20 s03 — P8 调度验证 + P9 二开评估收官

## P8
- schedule 常驻：`goose-scheduler`（acp --enable-scheduler，pc 托管）+ `schedule add/remove/list/run-now/sessions` 全通；**cron 到点自动执行**（每 2 分钟测试 job 自动开 session）。坑：recipe 必须有 `title` 字段（错误信息明确）；job ID 已存在需先 remove。
- 卸载干净度：系统唯一足迹 = AppData memory junction；新增 `卸载清理.cmd`（rmdir junction + 空父目录）。

## P9（ADR-0006）
六项已知缺陷全部运行时对冲，零 fork。二开触发线与"吸收重造优先"原则成文。
待办：goose memory 便携缺陷提上游 issue（记录在 STATE）。

## 环境备注
- pc/faucet 端口动态探测后可能非默认（读 data/pc.port、data/faucet.port）。
- 双 goose-scheduler 进程曾并存（残留），已清理；pc 托管的可被 restart。
