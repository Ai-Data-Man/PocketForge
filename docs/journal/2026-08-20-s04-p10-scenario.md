# 2026-08-20 s04 — P10 场景模拟完整测试（模拟妻子公司机）

## 测试环境构造（与开发机隔离）
- 全新路径 C:\PocketForge-Test 解压新包（sha256 49c8dad0…，174MB）。
- LLM 走公网端点 http://111.228.54.166:20128/v1/（模拟公司机外部 LLM）。
- fake PLM 用**包内 node** 起静态服务（不依赖开发机 python）。
- 全程 cmd 入口（无 Git Bash 依赖）。

## 结果（全部 VERIFIED-RUN）
| 步骤 | 结果 |
|---|---|
| 双击启动（全新解压） | ✅ nats/faucet/goose-scheduler/faucet-rawsql 全起 |
| 妻子任务 1：抓取 fake PLM → 入库 | ✅ 6 行 → parts.parts_report（公网 LLM 下 agent 自主完成） |
| 妻子任务 2：建看板应用 | ✅ 首版踩坑后自愈重做（node 静态服务 + faucet REST），浏览器渲染 6 行验证 |
| 整栈重启 | ✅ app 自动回归（apps 聚合）+ 数据 6 行仍在 |
| 卸载 | ✅ 卸载清理.cmd 删 AppData junction，便携数据完好 |

## 暴露并修复的真实缺陷（本轮核心价值）
1. **agent 用系统 python 建应用**（目标机没有）→ .goosehints 增"应用运行时清单"（只用包内 node/静态页/faucet REST）；agent 收到清单后自愈成功。
2. **faucet raw_sql 全新部署默认关**（旧环境是手动 UPDATE 的，新装即失效）→ 新增 `faucet-rawsql` oneshot 进程（node:sqlite，node 22 内置），依赖 faucet healthy 后自动置位；ADR-0004 的协议落地为代码。
3. **cmd 管道语法坑**（agent 跑 bash 风格命令失败）→ hints 增 Windows 命令注意事项。
4. **卸载脚本 rmdir 在部分环境不稳** → 重写为 cmd 壳 + PowerShell 核（验证通过）。
5. 打包混入运行时生成物（memory-mcp.cmd/diag.ps1/memory/生成 config）→ package.sh 排除清单扩充；data/secrets.env 模板化（OPENAI_* 同步写，交付时填一次）。

## 备注
- agent 建库命名（自建 parts service）与 hints 模板约定略有出入——可接受（协议是行为契约不是死规矩）；后续观察是否收敛统一。
- goose run 偶发长任务 >500s：真实使用是交互 session，不受影响。
