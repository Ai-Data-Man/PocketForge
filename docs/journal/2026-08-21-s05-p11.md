# 2026-08-21 s05 — P11 四能力增强（skill 自进化/备份/沟通/小白引导）

## skill 自进化（R6 官方规范 → 落地）
- 项目级 `.agents/skills/` 天然便携（无需 junction），goose v1.46.0 发现验证通过。
- 按 agentskills.io 规范写元技能 skill-sediment（三触发信号/四要素/三段式 description/修订优先/验证闭环），替换此前自创协议（用户点名要求学现成方案，已照办）。
- 实测闭环：agent 自主写出 scrape-table-to-db skill（830 tokens，质量合格）→ 新任务引用 skill 执行 → 6 行入库核对一致 → 人话汇报。

## 抗风险（备份）
- `forge-backup.tpl.js`（node + PowerShell Compress-Archive）：faucet/sqlite/js/conf 四源 zip，保留 7 份自动轮换；坑：Compress-Archive 多路径一次调用不稳 → 首路径 -Force + 其余 -Update 逐个追加。
- 编排：pc `daily-backup` oneshot，依赖 faucet-rawsql 成功后执行 → **每次启动自动备份**（妻子场景=每天开机即备份）。
- 恢复验证：备份 zip Expand-Archive 解出全部关键文件（faucet.db/.apikey/goose config/memory/recipes）。

## 沟通 + 小白引导
- hints 增沟通规矩：说人话/做完汇报/犯错直说/二选一提问/收尾词识别。
- 首启流：bootstrap 检测 data/welcome.done → 生成 welcome.html → 启动器 `start` 打开浏览器 → 改名 welcome.shown 防再弹。回归实测通过。
- forge-selfcheck skill（体检入口）：说"做个体检"即全栈健康检查。

## 回归（测试沙盒 C:\PocketForge-Test）
全栈六进程（含新 daily-backup）+ welcome 首启流 + 备份自动执行 + inventory-web 回归 + skill 随包复制生效，一次全过。

## 备注
- 备份含 faucet.pid（无害）；windows Compress-Archive 对 js/ 目录含 $ 符号子目录正常。
- R6 原始报告要点已入 research/06。
