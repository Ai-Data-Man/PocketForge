# 2026-08-30 s51d — P31-② 收窄落地：配 Key 引导条（GUI 全链路 VERIFIED-RUN）

## pf-pm 裁决（主控采纳）
- 独立向导页**裁掉**：⚙️ 面板本身已是完整配 Key 向导（档案/Key/拉模型/测试连通），再做向导=同一功能双入口。落地=「无 Key 检测 → 聊天区顶部引导条 → 打开现有面板」，一个组件+一次 modal 调用。
- P31-② 原设想（任务类型选择+预填流程）实质已被 s33 chips + s48 报表卡实现，冻结挂 P32（妻子一次没用过，无实证）。
- key 格式校验裁掉（假阳性拦截比放行伤）；不做「不再显示」持久化——条件是客观状态（没 key），配好即消。

## 实现（纯前端，chat.tpl.html，桥零改动）
- 判定点：`renderProviders`（WS providers 推送消费点，初始加载与保存后桥都推）调 `keyGuideSync(list)`：所有档案 hasKey=false（含零档案）→ 聊天区顶部插引导条（「还没连上大模型…」+「去配置」按钮→开 ⚙️ 面板并聚焦 #cfg-key）。
- 完成回调：任一 hasKey=true 且引导条曾显示 → 移除条 + addInfo('✅ 配好了！试试下面的任务') + addQuickPrompts()。
- 401 人话卡（s50e）保留为第二道网。

## GUI 实景验证（真 Edge + a11y，双态实测）
- A1 无 key（实验：备份 providers.json → 清空 key → 重启桥 → 刷新）：引导条出现 ✅
- A2 点「去配置」：⚙️ 面板打开、Key 输入框聚焦 ✅
- A3 填 key 保存：档案变 🔑、引导条消失、「✅ 配好了！试试下面的任务」+ chips 重现 ✅（同时触发 hotRestart，行为链完整）
- A4 预填包（hasKey=true）刷新：无引导条 ✅
- A5（401 兜底）/A7（重启后首任务）留沙盒全新部署与护航验证。

## 环境操作记录
- providers.json 为桥自有配置（非用户业务数据），实验走 备份→清 key→重启→GUI 还原路径；测试后已从备份还原（两档案 key 均在）并重启桥，临时 key 文件已删。

## 回归
- e2e-chat 26/26 + fuzz 23/23（还原后终态）。
