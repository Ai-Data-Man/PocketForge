# 2026-08-30 s53 — v0.9.8 打包 + 沙盒全新部署首任务冒烟（A5/A7 实景闭环）

## 打包
- tag v0.9.8（s51 系列 4 commits + s52 两项，8 commits since v0.9.7）→ `dist/PocketForge-20260830-v0.9.8.zip`（288MB，28553 文件，sha256 就位）。
- 包内特征全验：keyGuideSync / /api/db/_schema（桥+模板）/ showMtab+aria tablist / 文件树自动刷新（stop 分支 renderCurPane）/ gen-xlsx 标题修复 / VERSION=v0.9.8 / hints 含「数据库表」。

## 沙盒全新部署冒烟（C:\PocketForge-Test，先 down dev 栈防端口互斥）
- 全新解压启动：secrets.env 的 FORGE_AGENT_API_KEY 为**空**（全新用户真实形态）；faucet.port=8091（包默认）、桥 8790、pc 8099；db/overview 空态 {ok:true,services:[]}。
- **A1（无 key 引导条）**：打开聊天页 → 「还没连上大模型…」+「去配置」出现 ✅（真实全新用户第一屏）。
- **A5（401 兜底）**：无 key 直接发「你好」→ 401 错误 + 人话卡「💡 Key 没配上或不对…填好 Key 再回来点重发」双网并存 ✅。
- **A2/A3（配 Key 引导链路）**：点「去配置」→ ⚙️ 面板开、Key 框聚焦 → 填名称「公司中转」+ 地址 + Key → 添加 → 档案 0 模型·🔑 → 拉取模型列表得 69 个 → 勾 glm-5.2 → 保存 → 顶栏切「公司中转 · glm-5.2」+「✅ 已切到」提示；引导条消失 +「✅ 配好了！试试下面的任务」出现 ✅。
- 实验插曲：首次保存时 key 用的是占位值，重发报「Invalid API key」（区别于此前「Missing API key」）——反证 key 确实送达上游、错误分类准确。补真实 key 写入 providers.json → 桥重启（A7「重启生效」路径）。
- **A7（重启后首任务成功）**：刷新后无引导条（A4 沙盒复验 ✅）→ 发「你好，请用一句话介绍你自己」→ **真实回复落库**（「你好，我是小 forge——你电脑里的数字员工…」），工作区 ws-0830-040004 创建，会话命名「自我介绍请求」✅。
- **s50 部署缺口正式关闭**：全新包无 key 首任务 401 → 引导条（事前）+ 401 人话（事中）+ 配好即用（事后）三层闭环，全程零术语、零文档依赖。

## 环境注意
- 冒烟用 key 为开发栈公司中转 key（本机 9router 同源，沙盒可用）；冒烟后沙盒保持已配置状态（含 key），dev 栈待拉回。
- 沙盒 providers.json 手工注入 key 属测试便利；真实用户走 GUI 填 key 路径（本轮 A2/A3 已实景验证该路径）。

## 遗留
- 真机 POC 仍待用户（v0.9.8 即护航包）。
- 「✅ 配好了」确认条与「✅ 已切到」提示并存略重复（PM 备忘，下轮 UX 收敛候选）。
