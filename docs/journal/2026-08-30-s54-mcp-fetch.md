# 2026-08-30 s54 — MCP 商店补「网页抓取」（fetch-mcp），Backlog 收尾项落地

## 背景
- Backlog「MCP 商店最小形态」（s42 结论）：候选 = fetch + sequential-thinking。sequential-thinking/memory-graph 已于 s46 落地；本轮补齐 fetch。

## 选型取证
- 官方 @modelcontextprotocol/server-fetch **不存在 npm 包**（E404，官方 fetch 为 Python 包）。npm 实测选定 `fetch-mcp@0.0.5`（MIT，deps 8，stdio，tools: fetch_url / fetch_youtube_transcript）。
- 许可证注意：registry 声明 MIT 但包内无独立 LICENSE 文件、registry 无 repo URL——vendor-licenses/fetch-mcp.MIT 以「标注来源的 MIT 声明」落档（注明缺口），风险可接受（MIT 白名单内）。

## 实现（最小 diff）
- chat-bridge.tpl.js MCP_CATALOG 增一项 fetch（pkg/entry/license）；tpl→bin cp 同步。
- 安装/写配置/轮询/卸载链路复用 s46 既有机制，零新代码。

## 验证（VERIFIED-RUN，GUI 实景）
- GET /api/mcpstore 列出三项（fetch not-installed）。
- POST 安装 → 25s 后 stage=done；bin/vendor/mcp-fetch/ 落盘；config.yaml 追加 mcp-fetch extensions 块（绝对路径+包内 node）。
- 桥重启挂载后，聊天发任务「用 fetch_url 抓 fake-plm 页面」→ 工具卡「mcp-fetch: fetch url … 完成」→ 正确回答「缸体，数量 12」。第三方 MCP stdio 工具调用全链路真实生效。

## 回归
- e2e-chat 26/26 + fuzz 23/23；bridge node --check 过。
