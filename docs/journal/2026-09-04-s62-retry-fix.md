# 2026-09-04 s62 — s50e 换线重发失效取证与修复 + qa 建议级收尾 + 矩阵补跑

## 输入
s61 遗留三线：streamEl 死代码专项（pf-engineer 发现）→ 取证；qa 建议级两项；矩阵剩余条目。

## 主线：s50e「换备用线路再试」完全失效（P1，v0.9.8 带病）

**取证（pf-researcher，15/15 探针）**：双重缺陷叠加，重发一步都不执行（非发空）：
1. `dataset.orig` 死代码——`submit()` 内写点位于 `addMsg(user)` 之后，而 user 分支恒置 `streamEl=null`；重发原文设计来源恰是它，恒未写入 → 回落到错误文本。
2. `endStream` 局部 `const txt`（string）遮蔽全局输入框 `txt` → s50f 加的草稿守卫 `txt.value.trim()` 在 pendingRetry 闭包解析到局部串 → `undefined.trim()` TypeError → 重发不执行 + 中断 subscribed handler 后续（换线后模型名不刷新/工作区不绑定）。

**归属**：遮蔽=s50f 草稿守卫（469a6e9）引入；**漏测机制实证**——s50f-assert.js 用自造 `txt={value}` 对象桩执行「同构」逻辑，恰好绕开 endStream 真实词法作用域。教训：**探针桩环境必须复刻被测系统的真实作用域，接口同构≠词法同构**。死代码引入点在 s50e 终验后至 v0.9.8 窗口（未验证——项目目录无版本库）。v0.9.8 沙盒快照已含两缺陷（VERIFIED-DOC）。

**修复（pf-engineer，27/27 探针）**：txtRaw 解遮蔽 + 模块级 `lastOrig`（submit 记录实际发送值；subscribe/openSession 清空——切旧会话防串发，语义论证过 busy 挡板先于 lastOrig 写入，连发不覆盖）+ 消息级重发补按钮文字清理。qa 快审通过（P1/P2 清零）。

## qa 建议级两项落地（pf-engineer）
- rollbackQueue 草稿覆盖前 addInfo 全文展示（原草稿不再无声丢失）
- delete_session 回执 `writeFrame(...,cb)` 回调内 destroy（探针 A：旧形态同 tick destroy 丢 98.5% 数据/新形态 100% 冲刷；探针 B：真实桥回执先于断连 2ms 到达）

## 矩阵补跑（主控 GUI）
条目 5（258 字符长 URL）PASS——气泡内 word-break 换行，无横向滚动；条目 4（矮窗 1650×510）PASS——输入区/chips/消息区完整；条目 20 已 PASS。跳过：13（全归档需大改 dev 数据）、14（无 key 态 s51d+s53 已覆盖）、18（confirm 为浏览器原生 OS 对话框，非产品代码）。

## qa 快审 P3 留档（4 条，下轮）
1. delete_session 请求者 destroy 无超时兜底（建议回调外 setTimeout 5s.unref）
2. retry 正则 `/((复制|重发)\s*)+$/` 误裁正文尾字样（「帮我重发」→「帮我」；exportChat 同缺陷同正则——建议两处改剥离 .mbar 取正文）
3. 让位草稿 addInfo 无截断，超长粘贴撑消息墙（slice 截断）
4. WS delete_session 回执路径无专项 e2e 断言
另：消息级「复制」按钮同款按钮文字混入（:1664，独立跟进）。

## 验收
e2e 32/32 + fuzz 44/44 全绿；探针 27/27 + A/B。
