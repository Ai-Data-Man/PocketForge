# 2026-09-04 s63 — qa P3 留档批量清零（断线续跑收尾）

## 输入
s62 qa 快审 4 条 P3 留档 + 复制按钮文字混入。会话一度中断（栈停、engineer D 部分改动落地未验证），续跑收尾。

## 落地（五点）
1. **destroy 5s 超时兜底**（chat-bridge.tpl.js delete_session）：回执 write 回调外 `setTimeout(()=>destroy(),5000).unref()`——挂死对端不读时 socket 不滞留 allClients 至进程级；正常路径回调 destroy 后 timer 再触发幂等（二次 destroy 不抛，close 只发一次→drop 只清一次，注释已声明）
2. **retry/exportChat 正则误裁修复**：新增 `msgText(m)` 唯一入口——clone 后剥离 `.mbar` 操作条再取 textContent；exportChat/复制/重发三处统一改用（原正则会误裁以「重发/复制」结尾的正文，如「帮我重发」→「帮我」）
3. **让位草稿 200 字截断**：rollbackQueue 超出补「…（太长，只显示前 200 字）」，防撑爆消息墙
4. **WS delete_session 专项断言**：tmp/probe-delete-receipt.js 转正 tools/e2e/ws-delete-receipt.js，e2e-chat.sh 第 11 节调用，断言回执先于服务端断连到达（实测 33/33 含此项）
5. **消息级复制按钮**：同 msgText()

## 续跑注意事项
- 会话中断后 dev 栈整体停止（无 pc 进程）——重拉 dev-stack-up.ps1 恢复；bridge 重启正常。
- fuzz 首跑 1 红、热跑 44/44——**冷启动竞态第二次复现**（s61 同现象），统一观察：栈冷启后首跑 fuzz 偶发 1 红，疑似某断言依赖 goose 冷加载窗口未就绪；两次均未在日志留痕直接归因，留待专项（低优先——热跑稳定全绿，内测真机不跑 fuzz）。

## 验收
e2e-chat 33/33（+1）+ fuzz 44/44 全绿。