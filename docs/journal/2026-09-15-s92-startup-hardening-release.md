# 2026-09-15 s92 启动路径专项：反复测到干净 + v0.9.13 发布

用户指令（两轮）：①「依然会有各种启动就会出现的bug，请反复测试彻底解决」②「然后发一个release」。

## 用户实测驱动发现并修复（全部有独立复现实验，非推测）

| # | 现象（用户视角） | 根因（实验定案） | 修法 | 验证 |
|---|---|---|---|---|
| S-A | 管理员账户双击 → 「没连上数据库」 | 内置 Administrator 双击=高完整性令牌（该账户 UAC 过滤不生效，whoami /groups 实证）；PG 拒绝管理员运行 → 桥降级 file 态 | 启动器检测高完整性 → runas trustlevel 自动降权重开（检测判据=管理员组是否可用，与 PG 放行判据同源；完整性 SID 在受限令牌中仍存在，用它会无限重开窗——实测踩坑已修） | 端到端：高权限启动→提示→降权→postgres 进程在+桥 200+db/overview 200 |
| S-B | 点「停止」没反应；叉启动窗口停不掉 | 停止命令未重定向 stdin/未禁 TUI → down 客户端挂死；pc 是独立进程不随窗口死 | 停止脚本 stdin 加固 + 降级态（子进程死而 pc 残留）强制收尾 + 失败打印原因不再吞错 | 正常 <15s 全停；人为造降级态也能收干净 |
| S-C | 黑窗口被日志刷屏 | faucet 健康检查 http_get /healthz 每 5s 记一行 access 日志 | 探针改 TCP（pg-probe 同款先例） | 30s 增量 0 字节实测 |
| S-D | 每次启动出现红色报错（备份/数据库） | ①备份与桥同启，撞「库未建」②撞 PG「尚未开始接受连接」（postgres 对每次拒绝记 FATAL） | 备份与桥均加**真就绪门**（读 pg.log ready 行再发首个连接，零连接成本）；等待上限 40s 后信息级跳过 | 13 轮冷启全日志零异常 |
| S-E | 强杀/崩溃后重启满屏 EADDRINUSE + FTL | pc 死而子进程成孤儿占端口；双击二次启动产生双 pc 共用 pc.log（改名报错来源） | 启动器预检 start-preflight.ps1：单实例识别（栈在跑→友好提示退出）+ 孤儿清理（只清本安装目录） | 崩溃恢复 11s 新栈就绪零报错；重复双击 pc 恒=1 |

另修：rename 报错（双实例共用 pc.log 的日志轮换冲突）随 S-E 单实例识别根除；`.cmd` GBK wrapper 被启动器 chcp 65001 污染（测试基建侧，wrapper 先 chcp 936 修正）。

## 反复测试矩阵（全部为「反复」而建，可重跑）

- `tmp/s92/pf-boot-cycle.py`（N 轮：冷启→就绪→停止→零残留，全日志异常词典扫描）——**13 轮全绿**
- `tmp/s92/pf-edge-matrix.py`（6 场景：二次启动/停止空跑/快速启停/强杀重启/启动中开聊天/叉窗口后停止）——**6/6 全绿零异常**
- `tmp/s92/pf-e4-focused.py`（崩溃恢复逐步取证）——预检清理孤儿→新栈 11s 就绪→停止零残留

## 发布

（QA 发布门 + release 结果待本会话末节补录）

## 教训

- #31 待入册：console 代码页污染链——任何 chcp（含启动器的 chcp 65001）会改共享控制台状态，影响后续同控制台内的 .cmd 解析；测试基建 wrapper 必须自带 chcp 936。
- 复核：`pg_log ready 行`做就绪判据 = 零连接成本的就绪门（优于 TCP 探活：端口先于「接受连接」可用）。
  **❗ 上条被发布门推翻（s93）**：受限令牌启动路径下 `data/logs/pg.log` **恒 0 字节**（postgres 输出进 pc 日志缓冲/pc.log，pg.log 只在停机时落盘）——据此做门 = 门永不开 → 备份崩溃零产出（QA F1 阻断项）。最终判据=**TCP 探活（bin/pg-probe.js）+ 1.5s settle**（跨 recovery 窗口）+ 显式状态返回。

## s93 发布门（独立 QA 两轮）

- **第一轮**（QA 独立复测，tmp/s93-release-gate-qa.md）：G2/G3/G4/G5/G7 全绿；**F1 阻断**=daily-backup 每次启动崩（就绪门读永不写入的 pg.log → 空串当成功 → statSync ENOENT）+ 零备份产出；F2=v0.9.12 旧升级器 R3 现场（文档化）；备注=应用模板无重启上限。
- **修复**：就绪门改 TCP 探活（backup+桥双处）+ 显式状态与存在性守卫（绝不抛异常）+ 模板 max_restarts: 3 + 发布说明补 F2 手动收尾两步。
- **第二轮**（F1 复验）：F1 六断言全绿（exit 0/两库 dump/zip 含 pg-dumps/降级边界）；**F3**=首启 12 行 `FATAL: database "forge_bridge" does not exist`（备份盲试 dump，桥建库比窗口晚 2-3s）→ 修=先 psql 查库存在再 dump（零 FATAL）+窗口 50s。
- **第三轮**（F3 定点复验）：4 棵全新树，两库 dump 首启即产出、停机后全日志零异常。
- 方法学入册（QA）：**pg.log 只在停机时落盘**——启动期日志扫描必须在停机后做终态复扫，否则漏 pg 侧全部内容（自测漏 F3 的原因）。
- 教训 #32 候选：容器/编排下的日志落盘时机与令牌上下文相关，任何"读日志判状态"的设计必须先验证该日志在目标路径真的会被写。

## 发布（已中止，状态封存——用户 2026-09-15 22:0x 拍板停止发版，转入另一会话做全面整改优化）

- tag `v0.9.13` 已创建并推送（指向 7b8b14d）；GitHub release `v0.9.13` 已发布（assets: zip `ac0e6e745baef2adde643e727a51bc535ffc617c71ee3dcd6871f9b8ef4ca17b` 323,576,267B + .sha256，正文=精修发布说明）。**用户拍板中止后此 release 视为草稿态**——下一会话整改完成后应：删掉该 release 与 tag 重发，或在整改完成后直接发新版本号。注意 release 资产哈希与发布说明初稿不一致（见下）。
- 发布过程中额外发现并修复两个**流程缺陷**（与产品无关但影响交付可信度）：
  - **打包非确定性①**：`node.MIT`/`goose.Apache-2.0` 许可证从 GitHub main 分支拉取=活文档，同 commit 两次打包字节不同（实测 node.MIT +59 行）。修=改用包内来源（node 发行版自带 LICENSE）+ goose 许可文本钉定 v1.50.0 入库 `forge/vendor-licenses/goose.Apache-2.0`。
  - **打包非确定性②**：zip 条目携带文件 mtime → 内容全同的两次构建 sha256 不同。修=条目时间戳归一 `2026-01-01 00:00:00` + 权限位固定。**修后两次构建 sha256 完全一致（可复现构建达成）**：`5d56003e4854e1780485062cb57e751c159d3cab9c92a4270b293bd8ccc002e1`（323,575,360B）。
- 发布门独立复测三轮（QA）：一轮抓出 **F1 阻断**（备份崩溃零产出）；二轮确认 F1 关闭 + 抓出 **F3**（首启 12 行 FATAL，桥建库晚于备份窗口 2-3s）；三轮 F3 关闭（4 棵全新树：exit 0/两库 dump/停机后零 FATAL/zip 含 pg-dumps）。报告 tmp/s93-release-gate-qa.md + 证据 tmp/s93-qa/、tmp/s93b-qa/、tmp/s93c-qa/。
- 方法学勘误（QA）：pg.log 只在停机时落盘——启动期日志扫描必须在停机后做终态复扫。
- F2（v0.9.12 存量升级）已文档化：升级前备份 apps\ + 升级卡停时手动 runner 收尾（发布说明升级路径节）。

## 遗留（下一会话全面整改的输入）

1. **发布收尾**：GitHub release v0.9.13 草稿态处置（删/重发/换版本号）；发布说明指纹需更新为整改后的最终包；可复现构建已就绪（同输入同字节可自证）。
2. **启动期完整功能复测**：F3 修复后的包只做过定点复验（备份链+零 FATAL），完整功能面（会话/应用/聊天等）应在整改后的包上重跑一遍全功能验收。
3. **QA 抽检遗留**：「本版起后续升级 apps/ 零触碰」未做第二跳演练（UNVERIFIED）；pg.log 在受限令牌路径恒 0 字节的 pc 行为属上游特性（已绕开，可作为观察项）。
4. **测试约定**：AGENTS.md §8.1 已立——唯一沙盒根 `C:\PF-TEST\<场景>`，测试后整删；禁再创建散落临时目录；dev 树与用户树永不为沙盒。
5. dev 栈现停（PFdrill2 Ready 可重启）；用户树 `C:\PocketForge` 停留在 s90 版本（无 s91/s92/s93 修复），整改后应同步最新包。
