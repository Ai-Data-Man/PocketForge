# s96 工程小批：QA 复审 P3-1/P3-2/P3-3 收尾两切片

- 时间：2026-09-16 ｜ 角色：pf-engineer ｜ 上游：tmp/qa-s95-review.md P3 节 ｜ 与 s96 聚合守卫批（5fbebfa/554db36，并行工程师）同会话不同 commit

## 切片 1：桥 APP_INFRA_PROCS 同源化（P3-2+P3-3）——bafbb51

- 改动：forge/conf/templates/chat-bridge.tpl.js——`const APP_INFRA_PROCS` 由硬编码 9 键改为 IIFE 启动现读 `conf/process-compose.yaml` 顶层进程键（复用 appParseYaml 行级判据，与守卫脚本同款），读文件失败/解析空 → 回落硬编码 FALLBACK 9 键（fail-safe 不 fail-open）；启动一行 info（键集来源=现读/回落+键数+键名）。
- 消费点零改动：POST /api/apps 与 /api/apps/logs 的 `APP_INFRA_PROCS.includes(n)` 原样（读同一 const）。
- apps-probe B15 锚随机制升级：第三合取项由「字面 9 键常量」改为「IIFE 内现读路径+`keys && keys.length` 门+FALLBACK 字面 9 键」三重锚——QA P3-2 点名的漂移盲区（yaml 增第 10 键时旧锚不红）闭合。
- 验证：
  - 判据离线核对：appParseYaml 对真 yaml 提取 9 键与 FALLBACK 同集（同序不同序无关，includes 判定）；空文本 → `[]` 触发回落。
  - 物化：tpl→bin cp+cmp 字节一致；仅 `pc process restart chat-bridge`（healthz 200，node 直连 fetch）。
  - 回落演练：主 yaml 改名 → 重启（PID 驱动等待换新，不靠 sleep）→ `pc process logs chat-bridge` ring 出现「回落硬编码常量」行 + healthz 200 → 恢复 yaml → 再重启 → 「启动现读」行。两种形态键集均 9 键，deny 行为面一致。
  - apps-probe 69/69（B15 新锚含 A17 deny 负向量组）+ apps-fuzz 22/22 独跑绿。

## 切片 2：ia-logic-probe 补 chip 行为断言（P3-1）——b2e6af2

- 改动：tools/e2e/ia-logic-probe.js——sandbox 暴露 `skillsChip` setter；桩沙箱种 origin=self/null/market/local 四类技能各 1；C1（chip=self 命中自己攒的不命中 null 内置）/C2（builtin 判据=origin null）/C3（ext 判据=market+local 双中）/C4（all 对照 4 卡）。断言读卡片 innerHTML 聚合（桩 appendChild 不写容器 innerHTML——首版 C3 假红即此，修正后全绿）。
- 判别力实证（QA M4 恒绿缺口闭合）：变异 tpl `self→selfx` → C1/C2 红；`market→marketx` → C2/C3 红；git checkout 还原后 11/11 绿。探针总数 7→11 只增。
- 前端判据同源性：断言走提取执行路径（grab skillsOriginClass+paintInstalled 原文入沙箱跑），非字面锚——判据变更必然传导。

## 收尾

- e2e-chat 全量 61/61 零回归（含 apps 69ck）；fuzz 未重跑（两切片不触 fuzz 面，协调纪律豁免；apps-fuzz 无 APP_INFRA_PROCS 字面锚已核）。
- 残留自清：forge/apps/ 仅基线 e2e-report.yaml；conf/ 无 .hidden-tmp 残影；变异 tpl 已还原（git status clean）。
- 观察（非本批引入，留档）：pc.log 文件对 respawn 进程的 stdout 捕获滞后（首 spawn 有行、on_failure/手动 restart 的后续 spawn 无新行），桥启动日志可经 `pc process logs chat-bridge`（ring buffer）观测；回落演练时序必须 PID 驱动——pc restart 的换新进程可迟于命令返回 20s+（首次演练 sleep 3 误判 no-op 的教训）。
- 未 push（批次协调约定；仓库惯例 s85 拍板要求提交即推送，本批两 commit 待批次收口统一推）。
