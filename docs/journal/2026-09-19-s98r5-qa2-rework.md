# s98r5 QA 复审返工波（qa2，5 修项）

- 会话：pf-engineer ｜ 2026-09-19 ｜ 输入=tmp/qa-s98rework-review.md（P2-1/P2-2/P3/P4-1/P4-2 全文）
- 纪律：最小 diff 逐项修 + 红绿对照；全部改动先落模板再物化（bootstrap + cp + cmp 逐位一致）。

## 修项与红绿

1. **P2-1 桥侧旧词漏网**：chat-bridge.tpl.js faucet-db desc「存数据、查数据的本事」→「存数据、查数据的（保留它基本功能都在）」。
   顺手清扫（桥+conf/templates 妻子面可见串，QA 未扫到的同族）：`(远程技能)`→`(远程手艺)`；`先换个技能名`→`先换个手艺名`；`没有安装这个技能`→`没有安装这个手艺`；`商店里没有这个技能`→`商店里没有这个手艺`；market 回执 `这个插件正在安装/已安装`→`这个工具…`（工具目录技术区，插件无豁免居所）；welcome.tpl.html ③「学会的技能/添新技能/开关它的「本事」」→「学会的手艺/学新做法/开关它自带的」。
   semantics-r5-probe 扫描面扩到桥模板：LABELS/MCP_CATALOG/handleSkillstore 回执三段（剥注释扫串）+ 欢迎页整页 + **活体 /api/extensions 四行 desc 零旧词断言（QA curl 证伪形态进断言；桥不在线=SKIP 不计数）**，31→38ck。
   红绿：活体改前红（`[["本事"]]`，桥跑旧码）→ restart 后 38/38。
2. **P2-2 停启 rename 失败反射路径**：新增 humanToggleErr（humanDeleteErr 同款）——EPERM/EBUSY/EACCES→「有程序正占着这个文件，稍后再试」；ENOENT→「已经不在了」；未知→「没停成/没开成，稍后再试」；原始错误落桥 console，回执零路径。disable/enable 两分支接入。
   红绿：PowerShell 无 Delete 共享锁（QA M3 同款）改前红（回执含完整内部盘路径×2）→ 修后绿（人话句零路径）。
3. **P3 twin-guard 停用态内容陈旧**：update-runner apply 循环遇 .off 孪生：包内内容≠.off 现内容→写进 .off（状态保护不变，启用即得新版）；等内容跳过照旧；刷新失败不挡升级。回滚语义不变（.off 在 deleted 计划内，从备份还原）。
   r5-v2-upgrade 探针 11→15ck：层2-⑤ 保鲜写达 / ⑥ 等内容零写 / ⑦a 升级后仍停用 / ⑦b **停用→升级→启用=新版**（任务指定向量，真盘跑提取循环体）。
   红绿：临时还原旧守卫→⑤⑦b 红（13/2）→ 复原→15/15。
4. **P4-a restarted 回执缺 configOptions**：switch_model 跨档分支热重启后经 session/load（绑定 sid 的幂等 evicted-restore）取档位真相源随帧下发（ff6ef43 无会话分支同款；无绑定/回包无键=照旧不带），前端思考力度控件刷新窗口闭合。think-grad-probe +G6 桥锚，19→20ck。
   红绿：PF_BRIDGE=改前桥 G6 红（19/1）→ 修复后 20/0。
5. **P4-b op 类型混淆落安装分支**：skillstore POST 入口 op 严格枚举门（已给 op 就必须是 disable/enable/uninstall；123/[]/{}/未知串一律「参数不合法」）。
   红绿：改前活体 op:123/op:{}→「商店里没有这个技能」（落安装分支）→ 修后「参数不合法」。

## fuzz 增量（223→225）

- 228 重写：array op 在 op 门人话拒 + 市场未知名措辞钉「手艺」（同 ck 双 post）。
- +1：op 类型混淆（123/{}）人话拒。
- +1：停/启锁文件人话门——tools/e2e/skillstore-lock-fixture.ps1（ASCII-only，PS5 无 BOM 教训）无 Delete 共享锁占 SKILL.md/.off，disable+enable 两相回执均人话且零 `.agents`/EBUSY 反射。

## 验证矩阵

- 逐项红绿：五项全部改前红/改后绿（证据见上）。
- semantics-r5-probe 38/38（含活体）；r5-v2-upgrade 15/15；think-grad 20/0；audit-r5 14/14。
- 全量顺序：e2e-chat + fuzz-chat（新基线 225）+ ui-logic + ia-logic（数字见 STATE 本次条目）。
- 物化链：模板→bin cp+bootstrap 重物化 cmp 逐位一致（chat-bridge.js/update-runner.js）→ 仅 pc process restart chat-bridge → healthz ok。

## 未做（跟进项，不夹带）

- 使用说明.md 仍是旧词汇（本事/技能/插件多处，妻子面手册）——独立 rewording 批次，按 r5 母句口径过一遍再动。
- 桥报告头「已安装技能：N 个」（/api/report 诊断面）与 agent 面技能源字符串维持（裁决 §3 存活地：技术区/docs），未动。
