# 2026-09-26 s103 批3（收官）· S7 健康条出聊天流+去抖 / S8 S26 收窄

执行人：pf-engineer ｜ 裁决：docs/verdicts/2026-09-25-slow-session-observability-fix.md §3.3 ①② + §5 S7/S8 行

## S7 健康条永久退出聊天流+探测去抖（commit 2e4faed）

- 桥：`HEALTH_PROBE_TIMEOUT=15000`（8s→15s，/models 实测 1.6-9.1s 横跨旧线——慢≠不可用）；ok→非ok 迁移需
  `healthFailStreak` 连续 2 次失败确认（单次失败不翻 down 不广播）；恢复 ok 即时收；非 ok 间换态/null→非ok 直收。
  healthEvents 计数语义保持（去抖后的真态迁移才计）。events.log health_probe 行：未确认失败带 `pend=N`。
- 前端：healthSync 建条改挂 `#main`（#topbar 与 #chat 之间的顶部提醒条），不再是 `.msg` 聊天消息——聊天流只留
  对话实体；承载=条+侧栏连接点，零新第三处。
- 随迁：ui-logic-probe health 节 9ck（S7a/S7b 修前红锚）；health-probe-fuzz 24→27 asserts（F16 9s 判 ok /
  D1 单次不广播 / D2 连击确认 / D3 恢复即时；F15 hits=2；F21 适配去抖态机）；fuzz-chat.sh + e2e-chat.sh 标签随迁。
- 红绿（tmp/s103-e3-s7/）：红=red-bridge.log（slow12 单次→down，events `ms=8009 state=down`）+dom-red.log（S7a/S7b
  FAIL@HEAD）+fuzz-red.log（F16 旧 8s 语义红）；绿=green-bridge.log（慢12→`ok ms=12009`；单次 >15s 零广播+
  events `pend:1`；连续 2 次→down 广播）+fuzz-green.log 27/0。

## S8 S26 收窄（commit 54e03dd）

- **判定门先取证再定**（tmp/s103-e3-s8/arms-red2.log 活测五臂，真桥真 goose 假 provider 播控）：
  - fp（成功回复含 rate limit/timed out）→ 正常 stop，修前 upstream+1+rate+1+60s 复检回执（F5② 复刻红）；
  - SSE error 体 → goose 包成 `Ran into this error: …` **整轮文本**头冠居首，正常 resolve；
  - 401 → **JSON-RPC reject 路**（reject 分支既有分类+人话卡，非 resolve）；
  - 中途断流 → 残正文裸收零包装；plain 对照零计数。
  → 门=`S26_ERR_WRAP_RE=/^\s*(Ran into this error|Network error|Server error)/i`（错误报告头冠居首）。
- 桥：resolve 路与死 stop 通知分支换头冠门——成功 stop 零 upstream 计数/零 healthFailDebounce/零 sidErrAt；
  reject 路 :1599-1613 原样保持（401/真错误仍计数仍人话卡）；上游真实状态码由 S4 llmproxy 行承载（裁决明文取舍）。
- 前端：`endStream(abn)`——成功收尾只在头冠整轮文本画💡卡；断线路径 `endStream(true)` 保持原关键词判定。
- 随迁：toolcard-frames §9 提取容忍 (abn) 缺席=修前红可跑（S8a/S8d/F3f 3 FAIL@HEAD），51→55；assert-s50e +5 头冠单元断言。
- 红绿：红=arms-red2.log+dom-red.log；绿=arms-green.log（fp 三零：upstream=0/receipt=null；sseErr 保持
  upstream+1 rate+1；err401 reject 路保持 other+1+人话卡；midBrk 零计）。

## 验证与基线

- 每片独立物化链：bootstrap 物化+cmp 逐位一致+`pc process restart chat-bridge`+healthz 200。
- §8.2 全清单两轮（S7 轮/S8 轮）全绿：e2e 62/fuzz 225/ui-logic **92（83+9，基线升）**/ia-logic 38/semantics-r5 38/
  think-grad 27/capeditor 141/modelcaps 43/w2-preset 15/proxythink 30/llmproxy-xlate 10/w1-effort 14；
  另 toolcard-frames 55/rescue-guard 41/assert-s50e 9+5。
- 环境杂音实录（非 diff）：S7 轮 e2e 首跑 61/1=ws-close-reclaim 红——**泄漏沙盒桥进程**（首轮 fuzz-green 被 kill 残留）
  干扰探针进程树扫描，杀除后复跑 62/0；探针自清已复核（零残留进程/端口）。
- 手册：使用说明.md §46-49「顶部出现的提醒条」与新形态一致，grep 零影响零改动。
- 修复过程副产物教训：裸 WS 探针 `frame(JSON.stringify(...))` 双重编码致桥静默忽略（decode 出字符串 msg）——
  自建探针一律 `frame(obj)` 对象入参；shell 内联补丁反斜杠/`$'` 替换两踩坑（改用脚本文件+replacer 函数）。

## 未做/边界

- S26 正则最终形态（多关键词共现）=裁决 backlog（触发器：G1 数据显示异常收尾漏真错）。
- sid 复用竞态（④）、G10/G12/G13 维持 backlog 不动。
- push 随批协调落账（本批 +2 commit 本地未推）。
