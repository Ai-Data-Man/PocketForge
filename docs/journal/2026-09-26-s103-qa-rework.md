# 2026-09-26 s103 QA 返工批（终局批审 REWORK 三落 + P3-1 全臂复跑）

执行人：pf-engineer ｜ 输入：tmp/qa-s103-review.md（判 REWORK：P2×2 + P3-2 应修）｜ 主控修订裁决 3.1 条款（P2-2 信号源）

## P2-1 非 sk 形态 key 外带堵口（commit c97ed5d）

- 纵深两层：①写入侧 `logLine` 对 err 字段统一过 `sanErr`（黑名单 `Bearer\s+\S+` / `api_key[=:]\s*\S+` / `token[=:]\s*\S+` /
  `Authorization:\s*\S+`，命中片段替换 `<已脱敏>` 保其余错误文本可读；**Bearer 先于 Authorization 替换**——单遍正则里
  `Authorization:\s*\S+` 会把 "Authorization: Bearer abc" 吃成 "Authorization: Bearer" 残留 "abc"，两遍替换消除）。
  err 是上游错误原文唯一承载字段（test_model 错误体摘要/llmproxy 嗅探/switch 文案），evJson 与 llmProxyLine 同走 logLine，一处覆盖两日志。
  ②报告侧 `reportSanitize` 黑名单补同款四形态（兜修复前老行与其他入口，整行 `<已脱敏>` 既有语义不变，sk- 形态保持有效）。
- 红绿 tmp/s103-e5-qa/（新攻击值 e5br/e5ak/eyJlN + sk- 控制臂；红=HEAD 实跑）：红 6 FAIL（W1/W2a 行内原文 + G3a/b/c 报告
  LEAK-CONFIRMED + G4 qa- 旧标记行泄漏）；绿 13 PASS（行内零原文+片段 <已脱敏>+is invalid/provided 可读保留+sk- 控制臂
  原样在+请求方 key/host query 两防线保持+qa- 旧行被报告层新黑名单兜住）。
- report-probe-static 39/0 保持（扩展正则仍被探针抽取编译，8 形态命中/不误伤正常行均绿）。

## P2-2 阶段信号源换轨（commit e3fc72d）

- 事实基础变化（QA P2-2 + research/46 E0）：现行 W4 配置 goose 对所有非 gpt-5 系 0 thought 帧——原 S1「想阶段」永不触发。
  主控修订裁决 3.1：「想」=等模型响应窗口（prompt 发出→首个 tool_call/agent_message_chunk），effort 真上 wire=模型确实
  在 reasoning，**对所有模型统一显示**；假象边界=内容（无 thought 帧则 S2 面板照旧不出现，deepseek 条款同修订）。
- 实现 3 行：`setBusy(true)` busyPhase 初始 `''`→`'think'`（busyPhrase think 阶段档位人话链路既有，零改动）；
  thought 帧到达仍走 thoughtFeed（面板+阶段保持）；tool_call/message_chunk 翻阶段逻辑不动。两处头注随修订更新。
- 探针随迁：ui-logic-probe busy 节 3 断言（开忙即想/秒数递增/deepseek 想阶段保持——原「零想字假象」断言按修订反转），92 保持；
  busy-live-probe glm/deepseek 两臂期望同步修订（15s 内想阶段+档位人话+秒数递增；deepseek 无面板保持；thought 帧数只记录
  不设门）+沙盒 setInterval 改真实现（真页 1s 秒表机制，桩 fn=>1 表达不了想阶段秒数自走）。
- 红绿：红=HEAD 2 FAIL（T1 开忙=通用阶段/T7 同）；绿=8 PASS（它正在尽全力想·档位人话+秒表自走+work/write 翻转不受影响+
  thought 帧到=内容可见+收忙停表弃草稿）。

## P3-2 llmproxy 行补 path 字段（commit c265554）

- `llmDone` 行增 `path: req.url 路径段`——/chat/completions vs /models 一眼可辨；行为零改动只加字段。
  goose 自发 /models 目录刷新与 S7 健康探测的 model=""/effort="" 行从此不稀释聊天请求归因（RCA §4.3 建议 backlog 升应修）。
- 探针随迁：tmp/s103-e2-s4 行格式断言七键→八键 + path 归位新断言（6ck）；llmproxy-xlate 无撞（configOptions 翻译面，不触行格式）。
- 红绿：红=HEAD 2 FAIL（行内无 path）；绿=p32 探针 3/0 + 全臂 s4 6/0（/models 1+/chat 2 归位，200 行 model/effort 保持，4xx err 保持）。

## P3-1 全臂复跑（终态桥活体证据）

- busy-live-probe 五臂全绿：red 5/0、zombie 5/0、inject 6/0、**glm 12/0**、**deepseek 11/0**。
- glm 臂 12/0 含修订后期望（prompt 后 15s 内「它正在尽全力想」+档位人话+秒数递增；首 message 5.5s 翻「写答案」；
  草稿弃置收口）——QA 实录的 9 PASS/3 FAIL 三红全消；thought=0 如实记录（现行 W4 配置环境事实非缺陷）。
- deepseek 臂 11/0（「它正在深想」在场+秒数递增+**无 thought 帧=面板不出现**内容不造假）。探针会话自清（sessions.db 回 96 水位）。
- LLM 预算 flash 3 轮封顶使用：s4 B 臂 + glm 臂 + deepseek 臂。

## 回归与环境

- §8.2 全 15 套绿、基线不破（tmp/s103-e5-qa/reg-*.log）：e2e 62/62、fuzz 225/225、ui-logic 92、ia-logic 38、semantics-r5 38、
  think-grad 27、capeditor 141、modelcaps 43、w2-preset 15、proxythink 30、llmproxy-xlate 10、w1-effort 14
  （Part B SKIP=PROBE_KEY 未设，同 QA 基线形态）+rescue-guard 46、toolcard-frames 55、health-probe-fuzz 27。
- 每项独立物化链：bootstrap 物化+cmp 逐位一致+pc process restart chat-bridge+healthz 200（chat.tpl.html 运行时直读无需重启）。
- events.log 攻击标记行处置（QA 授权判断）：**手术式摘除**而非尾部截断——qa- 旧标记 12 行 + e5 返工红绿臂 8 行共 20 行
  按攻击 tag（*-model 名）精确删除，全部合法运维行（health_probe/switch_model 等）保留，保留行全 JSON 校验通过，grep 零残留。
  理由：截尾会误删攻击行之后的真实运维行；报告侧新黑名单本就能兜住旧行（G4 已证），清档只为消除 key 形状字符串长期驻留。
- 报告文件：本批攻击探针自产报告已自删（红绿各 1）；e2e 回归产报告按工程历史先例保留。explorer 窗口零残留。
- 环境：dev 栈 healthz 200；C:\PF-TEST 空。

## 未做/边界

- P3-3（down 二次确认无独立调度）QA 判可接受留档——写入回归手册「健康条语义」节归主控后续，本批不动。
- P4 系列维持留档；P4-4（120 截断劈半）对**新形态**实际更稳（已验证）：调用点先 slice(0,120)、sanErr 在 logLine 后置，
  但 `Bearer\s+\S+`/`api_key[=:]\s*\S+` 匹配任意长度非空残段——截在 key 中间仍命中（"Bearer e5br-BEA"→<已脱敏>）；
  截在关键词内部（"Bear"）残段不含 key 物质。sk- 形态维持 QA 原判（碎片无价值，非实际泄漏面）。
- goose 恢复 glm thought 帧通路（家族路线重评估/上游修复）仍留专项；现行修订口径下用户可见性已闭合。
