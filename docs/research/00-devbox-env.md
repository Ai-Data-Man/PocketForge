# 开发机环境探针（dev box）

- 结论：本机可作为 P2 验证场；目标机差异点已标注。
- 一句话用途：记录验证环境能力，避免把开发机特有设施误当目标机能力。
- 验证状态：VERIFIED-RUN（2026-08-19，命令见 journal s01）

## 事实清单
| 项 | 结果 | 目标机预期 |
|---|---|---|
| HTTP 代理 | `127.0.0.1:7890` 可达 github（CONNECT 200） | 无（交付包须预下载全部组件） |
| Edge | `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe` 存在 | Win10/11 企业版标配（关键假设，见风险） |
| WebView2 Runtime | 151.0.4129.x 存在 | Win10/11 多数预装（goose 桌面端依赖时需复核） |
| Chrome | 无 | 不假设存在 |
| 系统 Python | 3.8.6（过老，不用） | 不假设存在 → 用便携 embeddable |
| Node | v24.14.0（开发栈用，产品不依赖） | 不假设存在 |
| VC Runtime | msvcp140/vcruntime140 存在 | Win10/11 通常随更新预装（UNVERIFIED，标注） |
| 端口 4222/7680/8080 | 空闲 | 交付时若冲突需改端口机制（设计点） |
| 已占端口 | 8787/9119/20128（既有 AI 栈） | 无关 |

## 对本项目的关键事实/风险
- 开发机有代理与 Node，是"富环境"；一切验证结论写明是否依赖富环境。
- 系统浏览器复用假设（Edge 存在）在目标机上的成立性：Windows 10/11 企业版默认随系统分发 Edge —— VERIFIED-DOC 级常识，交付前在妻子电脑上确认。
