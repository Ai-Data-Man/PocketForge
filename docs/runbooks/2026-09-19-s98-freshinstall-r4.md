# Runbook：s98 主线5 从零安装反复测试（R4 三支柱 + 去重 + 报表卡双源 + 思考力度 全量出厂验证）

> 前置：E-A/E-B/E-C/E-D 四工程批全绿 + QA 复审通过后执行。测试模型 = 9router `glm-5.3-flash`（用户指定）。
> 纪律：AGENTS.md §8/§8.1——沙盒唯一根 `C:\PF-TEST\<场景>`，出厂 zip 全新解压，`.cmd` 启动器冷启，测后整删 + 零进程/零端口核对（8790/8091/8099/5432/4222/8222）。

## 包

- 从 `tools/package.sh` 新打 iat 包（版本号维持当前 VERSION，包名戳 iat 序号；不入发布序）。**打包前核对**：SKIP 已含 tmp/apps（forge/tmp/tf35 探针不进包）；bootstrap 重物化 cmp 逐位一致。
- 包内抽检：chat.tpl.html 含「每页」「一页看全」「数据库表」「出厂的」；chat-bridge 物化件含 official 字段逻辑。

## 每轮观测点（浏览器控制台零 JS 错误是硬门；日志逐段翻看）

1. **冷启链**：启动器预检→pc up→open-when-ready（healthz→打印地址→welcome→聊天窗）；收敛段（forge-register converge）先于开窗；oneshot 链全 Completed（backup/faucet-provision/rawsql/pg-init）。
2. **日志**：data/logs/ 与 pc.log——零 FATAL；已知良性 WARN 单条供给前瞬态；启动收敛行序正确。
3. **本轮新面（逐个点开）**：
   - 报表卡：双来源选择器（工作区文件 + 库里的数据表）；选表发送组装「数据库表 svc.tbl」；手填降级。
   - 页大小可调：任一翻页清单（apps/sched/mem/db/侧栏/浏览全部）改档→回第 1 页；重开面板记住；「一页看全」>500 截断提示（造数据或跳过——数据不足时记 N/A）。
   - mem 面板：分页器在场，「显示更多」不存在。
   - 插件 tab：官方徽标在场；技术区只有自加条目且零状态字段；h3/fnote 新文案。
   - 技能弹窗：chips「📦 出厂的」。
   - 浏览全部：搜索框输入即过滤（S3c 热修）；排序切换生效。
   - 批量操作（若 E-C 已落）：工作区整理模式勾选+双确认；归档会话批量删除（含当日号段拒批人话注）；记忆分类清空。
   - 思考力度（若 E-D 已落）：控件在场；glm-5.3-flash 诚实降级文案（调不了快慢）；若有支持模型则五档切换。
4. **回归面**：五端点 200；配置流（拉模型/保存/切换）；🚀 小应用注册全链（iat13 先例）；台账/本事/定时/数据表各面板翻页正常。
5. **卸载线**：停栈→目录可改名可整删；Edge 锚定窗零残留。

## 轮次计划

- R1（全量冒烟）：冷启→配置→真任务「搭个常驻备忘应用」（glm-5.3-flash）→新面逐项点验→卸载线。
- R2（新面深挖）：专打 R4/去重/报表卡/思考力度的边界（空态/大数据态——种 60+ 工作区与 500 记忆的膨胀场景验证三支柱）。
- R3（回归确认）：修完 R1/R2 发现后复验一轮。
- 每轮产出：tmp/s98-iatN-findings.md（现象/复现/日志摘录/初判级别），修复后红绿对照再入下一轮。

## 已知观察项（不重复立案，遇到新形态才追）

- 工具卡 GBK 乱码（P3 backlog）；pc.log 4KB 缓冲 ACP 计数滞后；配方 A 挂起残角（research/34 §五）；hard_stop 后 node 型孤儿（s91）。
