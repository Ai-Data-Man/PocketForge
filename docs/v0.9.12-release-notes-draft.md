# PocketForge v0.9.12 发布说明（草稿，发版时机待用户拍板）

更新日期：2026-09-13（s78 工作会话产出）。基线：e2e-chat 57/57 + fuzz 150/150（基线自 v0.9.11 的 55/150 升档，新增竞态/重试断言节）。

## 给使用者的话（正文用）

这次更新主要修了三件你可能遇到过的事：

1. **服务商配置再也「配不上去」的问题修好了**——以前添加新的服务商档案后，页面会悄悄把表单切回旧的那家，导致模型列表拉错地方。现在表单会一直停在你正在编辑的那家。
2. **线路坏了会主动告诉你**——聊天窗口顶部新增提醒条：模型服务商联系不上、或你用的模型已被服务商下架时，打开页面就能看到，点按钮去换，不用再「发消息石沉大海」。中转站 09-12 一夜下架模型名的实事故就是这个功能救场的场景。
3. **发消息偶尔会悄悄落错对话的 bug 修好了**——刚打开应用的两秒内点进旧对话，消息可能发进一个空对话且无任何提示。现在有代际守卫，不会串。

另外：大白话解释更稳了（模型偶尔「没说出话」会自动重试一次）；工具卡和权限卡里的长密钥改为打码显示；给妻子的使用说明补齐了常用话术（Tab）、✨优化、消息三按钮、顶部提醒条的说明。

## 变更明细（技术面）

### 修复
- 服务商面板表单重绑陷阱（GUI 实证：添加后 300ms 表单被重置回活跃档→拉模型打到错误 host）+ 同名档案重添加覆写凭据守卫 + secrets.env 回写自愈（NUL 损坏行不再永久存活）+ list_models https 支持（bb9c6b6）
- 双 subscribed 乱序竞态（P1：开应用 2 秒内点进既有对话→消息静默落错会话；wsPendingNew 代际守卫覆盖三发起点）+ 跨会话串台面（bindWs 成员籍+sid 过滤广播+permission 帧豁免）（6d94f3a/e9f04ee）
- 大白话解释/✨ 优化间歇空回（deepseek 隐形推理耗尽 300 预算：300→800+length 空回重试 1600；真链 7/10 空→10/10）（6d94f3a）
- readJsonBody 签名位置绑定灾难（全部 POST 端点连接重置）当日热修（b4e02d2）

### 新增
- provider 健康探测（裁决 2026-09-12-provider-health-probe）：GET /models 四态探测×三触发（页面打开/回合失败 60s 防抖/启动 90s）×常驻告警条（ok 态零视觉）×零自愈零计数（s50e 边界）；模型名硬编码全仓清零（两次断链事故的共同放大器拆除）（22732d1/e9f04ee/3f80ca9）
- 工具卡+权限卡 API key 显示层掩码（前6后4；喂料/回放/导出零改动）（3f80ca9）
- e2e 新三节：ws-subscribe-race（24ck）/explain-retry（13ck）/toolcard-frames 扩 31ck；套件 set -e 两处静默中止雷排掉

### 重构（行为零变化，A/B 逐字节实证）
- chat-bridge 模板 3743→3587 行：POST 样板 17 处收敛（readJsonBody/json200）、faucet REST 三胞胎合并、三大巨型端点顶层化（逐字节平移核对）、explain/optimize SSE 双胞胎合并（llmStreamOnce）；探针锚点零破损
- .gitattributes 钉模板/探针 LF（autocrlf checkout 幻影根治）

### 已知问题与挂账（见 STATE 开放问题）
- faucet v0.1.13 高危安全修复升级待用户拍板（fail-closed 破坏性迁移，需先建角色规则）
- faucet MCP query 注册表 stale（上游缺陷，agent 自然绕行=python/HTTP）
- goose v1.46→v1.50 升级评估通过（research/23），实施归下一版本窗口

### 升级路径
- PROTECTED 语义不变：自 v0.9.11 起受保护文件零触碰；v0.9.10 及更早存量升级仍需手动备份 conf/goose/config/（发布说明同 v0.9.11 口径）。
